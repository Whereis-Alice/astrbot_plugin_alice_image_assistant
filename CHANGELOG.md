# 更新日志

## v1.8.0 - 2026-09-15

- 新增 Yandex 图片反搜策略：通过公开图片搜索页面解析 `initialState.cbirSites.sites`，不需要 API Key；支持可选 Cookie、`.com -> .ru` 回退、CAPTCHA/页面结构异常降级和缩略图下载。
- Yandex 接入统一的跨引擎去重、位次评分、Dashboard 策略选择和自然语言意图路由；“找相似图”会优先尝试 Yandex，再按可用引擎回退。
- 配置面板新增 `enable_yandex`、`yandex_max_results`、`yandex_use_ru_fallback` 与敏感字段 `yandex_cookies`；补充解析脏数据、限量、Cookie 脱敏和回退测试。AnimeTrace 仍不加入本插件。
- Yandex 的独立实现参考了 [OMSociety/astrbot_plugin_reverse_searcher](https://github.com/OMSociety/astrbot_plugin_reverse_searcher) 的公开使用思路，未复制其代码或引入 pyquery 依赖。

## v1.7.0 - 2026-09-15

- 借鉴同类反搜插件的“按意图选择引擎”交互思路，独立加入现有反搜系统：`/aa溯 出处`、`/aa溯 相似图`、`/aa溯 角色` 等自然语言参数会在已加载的 SauceNAO、Google Lens、Ascii2d 中选择更合适的一条；LLM 工具 `alice_image_reverse_search` 新增可选 `intent` 参数。
- 显式传入 `strategies` 始终优先于意图路由；首选策略未配置时会按用途回退到其它已加载策略，无法识别意图时保持原有的全策略并行行为，不新增 AnimeTrace、Yandex 或 E-Hentai 依赖。
- 同步更新 `/aa` 帮助、Dashboard 指令目录与 README，并为路由优先级、不可用策略回退和兼容行为补充测试。

## v1.6.1 - 2026-08-29

- 修复 WebUI 主题菜单被页面内容遮住、选项点不动的问题。顶栏与标签栏都用了 `backdrop-filter`，各自形成独立层叠上下文，菜单上的 `z-index` 只在顶栏内部有效，实际绘制顺序仍排在标签栏与概览卡片之后，于是菜单一半被盖住、点击也被下层元素接走。现已给顶栏与标签栏显式排定层级，六个主题选项在标准与紧凑两种密度下都可见、可点。
- 概览页运行时卡片去掉「Web 后端」「Playwright」「VLM 提供商」「已注册提供商」四行，这些信息健康检查里已经给过，卡片只保留图片上下文会话与缓存图片两项。

## v1.6.0 - 2026-08-29

- 新增 Dashboard WebUI 工作台：在 AstrBot 面板内直接配置全部选项、查看 48 条指令说明，并试跑精确找图与以图搜图溯源。内置 `wonderland`、`aurora`、`midnight`、`sakura`、`paper`、`sunset` 六套主题；可用 `webui.enabled` 关闭，挂载失败时自动降级，不影响插件其余功能。
- 新增插件 logo，AstrBot 插件面板可直接显示；同时提供 64/128/256 三种尺寸与 SVG 源文件。
- 精确找图新增全分辨率终选复核。拼图每格只有 300x300，细节几乎全丢，因此选中的候选会再用接近原始分辨率的单图做一次结构化判定（是否匹配 / 置信度 / 理由），把“缩略图看着像、细节其实不符”的结果挡在发送之前。搜图神器、SerpApi 与 Pixiv 三条线共用同一套语义：明确否决则剔除并按上限重选，复核自身报错则保留候选交给 `fail_open`，绝不因技术故障丢图。
- 新增终选复核相关配置 `final_verify_enabled`、`confidence_threshold`、`final_verify_max_edge`、`final_verify_retry_limit`，均可在 WebUI 中直接调整，改动后立即对全部图源生效，无需重载插件。
- 新增检索词改写。用户的口语化描述会先整理成更适合各图源的检索词，改写结果只用于检索、不参与匹配判定，避免改写偏差污染筛图结论。
- 新增基于感知哈希的近似重复过滤与跨源候选择优评分。多来源并发返回时按匹配质量排序择优，而不是先到先发；同一张图的不同尺寸与不同站点副本不再重复占用候选位。
- 以图搜图子系统修复 22 项问题并新增结果排序模块，重整 SauceNAO、ascii2d 与 Google Lens 三条策略的错误处理与结果去重。
- Pixiv 线补齐质量控制：新增查询规划模块，修正标签解析、随机搜索、作品筛选与本地数据库相关缺陷，LLM 工具与插画处理器的行为保持一致。
- 工程化：引入 ruff 配置并保持检查全绿，测试规模扩充到 284 项（含 187 个子测试）；补齐 `.astrbot-plugin/i18n` 的中英文案；移除已废弃的 `@register` 装饰器，改用类文档字符串提供 `/plugin` 帮助信息；版本号统一从 `metadata.yaml` 读取；补上 `requests` 显式依赖与两处缺失的请求超时。

## v1.5.0 - 2026-08-14

- 新增默认开启的“当前会话 Bot 筛图”。审核模型留空时，Pixiv、搜图神器和 SerpApi 会继续使用当前会话模型，并额外继承当前人格与可配置的近期对话，从而理解“刚才那个角色”“要真实照片”等会话内指代和偏好。
- LLM 工具调用会直接复用本轮 Agent 的实时消息，包含当前用户请求；手动指令则回退读取持久化会话。筛图调用强制禁用工具，不会再次调用 `alice_image_find`，也不会把内部筛选结果写入聊天历史。
- 新增 `current_session_bot_enabled` 与 `current_session_context_turns` 配置。显式选择独立审核模型时继续保持原行为，不注入当前 Bot 人格或会话内容；独立审核模型失效并回退当前模型时也保持隔离。
- 兼容 AstrBot 4.16–4.17 的分支人格读取，并同步继承人格预设对话与 WebChat 特殊默认人格；传给 Provider 的筛图上下文会逐次复制，避免并发审核互相污染。

## v1.4.1 - 2026-08-13

- 修复搜图神器视觉审核只读取 `result_chain`、忽略 AstrBot 标准 `completion_text` 的兼容问题。部分 Provider 明明已返回选图结果，旧逻辑却会误判为审核技术失败，并在 `fail_open` 开启时放行无关首图；现在优先读取 `completion_text`，缺失时再兼容 `result_chain`，只有两者都为空才判定为技术失败。

## v1.4.0 - 2026-08-13

- 修复视觉模型明确判定“全部不匹配”后仍发送首张候选图的问题。新增默认开启的 `strict_match_enabled`；开启时明确否决始终拒发，与 `fail_open` 无关，`fail_open` 只在审核模型不可用、超时或响应解析失败时生效。
- 搜图神器改为渐进式分批筛选。首批候选全部不匹配时会继续审核后续候选，默认最多 3 轮；全部轮次仍无匹配结果时切换下一来源，关闭来源回退时则诚实返回无匹配图片。
- SerpApi 与 Pixiv 同步区分“没有匹配图片”和“审核技术失败”，并补上单图候选审核，避免换源后再次盲发首图。
- 普通实体和真实照片的 `auto` 回退顺序调整为搜图神器 → SerpApi → Pixiv，避免通用图源失败后优先落到插画来源；二次元查询仍优先 Pixiv。
- 新增搜图神器配置 `review_rounds`，用于控制最多审核轮数。

## v1.3.0 - 2026-08-04

- 新增 `randomize_search_results` 开关，默认随机抽取所有多候选 Pixiv 搜索结果；LLM 视觉审核会在打散后的候选池中继续按描述精确挑图。
- 新增按 `event.unified_msg_origin` 隔离的近期作品去重，覆盖 LLM Pixiv 找图、普通标签搜索、深度/AND/相关/推荐/排行榜搜索以及画师作品入口，不会影响作品 ID 或链接直取。
- 新增 `recent_dedup_enabled` 与 `recent_dedup_retention_days` 配置。候选池全部命中历史时自动允许复用旧作品，避免因去重导致无图可发；发送一开始即记录作品，因此平台确认较慢也不会触发同图重选。

## v1.2.0 - 2026-08-03

- `/aaP画师作 <用户ID> [数量]` 支持单次指定返回作品数；省略时继续使用配置项 `return_count`，不会修改持久配置。
- 新增 `/aaP画师随 <用户ID> [数量]`，从指定画师近期多页作品池中随机取图，并继续应用已有 R18、R18G、AI 和互动数过滤。
- 新增功能开关 `artist_random`、屏蔽标签配置 `artist_random_blocked_tags` 和作品池页数 `artist_random_pages`。屏蔽标签精确匹配 Pixiv 原始标签或翻译标签，不区分大小写。

## v1.1.0 - 2026-07-30

- 所有公开指令统一从 `爱图` 前缀迁移到更简洁的 `aa` 前缀，例如 `/aa`、`/aaP` 和 `/aa溯`；移除不再需要的 `alice-*` 指令别名。
- `/aaP <标签> [数量]` 支持在末尾指定本次返回作品数，范围为 1-10；省略数量时继续使用配置项 `return_count`，且单次覆盖不会修改持久配置。
- `/aaP画师找 <画师> | <关键词> [数量]` 同步支持单次返回数量；Pixiv 帮助文案、Fanbox 提示和以图搜图补图识别均已换用新指令。

## v1.0.4 - 2026-07-29

- 扩展 Pixiv 作品链接自动解析：链接可以夹在普通文字中，并兼容 `/en/artworks/` 等语言路径、查询参数、片段、末尾标点、移动端域名、无协议链接和旧版 `member_illust.php` 链接。
- 一条消息包含多个作品链接时只处理第一条有效链接，避免自动解析连续刷图；原有 `url_lookup` 与 `pixiv_urlsearch_enabled` 开关保持不变。

## v1.0.3 - 2026-07-29

- `alice_image_find` 新增可选参数 `artist_name` 和 `pixiv_user_id`。填写后会限定在指定 Pixiv 画师作品中找图；未填写时普通找图流程不变。
- 新增 `/爱图P画师找 <画师名或用户ID> | <关键词>` 指令，便于手动测试指定画师找图；关键词可省略。
- 新增 Pixiv 功能开关 `artist_search`，可单独关闭指定画师精确找图。
- 作者限定找图不会回退到搜图神器或 SerpApi，避免“指定 P 站画师”失败后发送无关图源。

## v1.0.2 - 2026-07-29

- 修复 LLM 自动找图时 Pixiv 已找到候选图、但平台发图确认超时后被误判为“找图失败”的问题。现在只要 Pixiv 已找到并开始发送，就不会再切换到搜图神器或 SerpApi，避免发出不相关图片。
- 新增发送状态字段：`warnings`、`send_attempted`、`delivery_uncertain`、`pixiv_found_count`。模型可以区分“搜索失败”和“已找到但发送确认较慢”。
- 新增配置 `find_image.tool_send_wait_timeout_seconds`。默认等待平台发图确认 45 秒；超时后工具先返回，图片发送任务留在后台继续。设为 `0` 可恢复一直等待发送结束。
- 保留用户手动设置的 Pixiv `original` 画质；仅把空配置时的运行时默认画质与配置面板默认值统一为 `medium`。
- 增加回归测试，覆盖“Pixiv 找到但发送超时不回退”和 Pixiv 画质默认值一致性。
